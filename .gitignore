# deps
node_modules/
.pnp
.pnp.js

# build
dist/
build/

# env — NEVER commit
.env
.env.local
.env.*.local
*.env

# vercel
.vercel

# secrets
vs-api-key.txt
proxy/config/default.json
proxy/test-*.js
qbtc-protocol/qrbtc-api/test-*.cjs
qbtc-protocol/qrbtc-api/_*.cjs

# ledger data (large + has chain hashes)
ledger/supabase-sessions-backup.json
ledger/sessions.jsonl

# passes (private chain data)
passes/

# OS
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*

# local only
*.local

# Noise — never commit
erslynnhquantumpass
*.bak.*
.env.local.bak*
qbtc-protocol/
QUANTUMPASS-v1.0-beta-VERIFYD-AUDIT*

# BuilderBro — keep source, ignore runtime store + logs
builderbro/rag/store/
builderbro/self-improvement-log.jsonl
